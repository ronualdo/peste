apiVersion: apps/v1
kind: Deployment
metadata:
  name: peste-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: peste-api
  template:
    metadata:
      labels:
        app: peste-api
    spec:
      containers:
      - name: peste-api
        image: ronualdo/peste:1d2756d2c7bbf725be5fa0b8b6a2bd81e6d6f116
        ports:
          - containerPort: 4000
        env:
          - name: SECRET_KEY_BASE
            valueFrom:
              secretKeyRef:
                name: peste-secrets
                key: secret-key-base
          - name: PESTE_BOT_TOKEN
            valueFrom:
              secretKeyRef:
                name: peste-secrets
                key: peste-bot-token


